
wget http://www.mcs.anl.gov/research/projects/mpich2staging/goodell/downloads/tarballs/1.2.1/mpich2-1.2.1.tar.gz

#this is downloaded so that we can build it and try out some of the example programs (which I couldn't find in)
#the centos repository. NEVER execute 'make install'. Only execute './configure' and 'make'. Then you
#can find the test programs in the 'example/' folder.
